apiVersion: v1
kind: ConfigMap

metadata:
  name: prometheus-config
  namespace: monitoring

data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:

    - job_name: 'infraready'

      static_configs:
      - targets:
        - flask-service.infraready.svc.cluster.local:80
